{
  "name": "bieon-iot-system",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "microservices/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=@bieon/backend-api\" \"npm run dev --workspace=@bieon/frontend\"",
    "build": "npm run build --workspace=@bieon/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "aedes": "^0.47.0",
    "mongoose": "^9.5.0",
    "mqtt": "^5.15.1",
    "net": "^1.0.2",
    "puppeteer": "^24.42.0"
  }
}
